# Landing hero screenshot

Drop a production dashboard capture here as `cadence-dashboard.webp`.

The landing page shows this image when present; otherwise it falls back to the animated in-app mock.

## Logo strip (optional later)

Add SVG or PNG logos under `public/landing/logos/` and update `src/data/landingSocialProof.ts` if you switch from text pills to images.
